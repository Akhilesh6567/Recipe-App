import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    scrollableView: {
        alignItems: "center",
        width: "100%",
        height: "auto",
        paddingTop: 30,
        paddingBottom: 50,
    },
});

export default styles;
