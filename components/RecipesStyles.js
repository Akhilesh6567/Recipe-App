import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  scrollableView: {
    alignItems: "center",
    width: "100%",
    height: "auto",
    paddingBottom: 100,
  },
});

export default styles;
